<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rrbofficial.btrapplock">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".applock" />
        <activity android:name=".flashlight" />
        <activity android:name=".fact_act" />
        <activity android:name=".notes" />
        <activity android:name=".gcoea_wall" />
        <activity android:name=".camera_act" />
        <activity
            android:name=".youtube"
            android:label="@string/title_activity_youtube"
            android:theme="@style/FullScreen" />
        <activity android:name=".deviceid" />
        <activity android:name=".deviceinfo" />
        <activity android:name=".operator" />
        <activity android:name=".sms" />
        <activity android:name=".phonedetails" />
        <activity android:name=".inbox" />
        <activity android:name=".contacts" />
        <activity android:name=".developers" />
        <activity android:name=".rone" />
        <activity android:name=".mydevice" />
        <activity android:name=".phone" />
        <activity
            android:name=".gcoeasite"
            android:label="@string/title_activity_gcoeasite"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".voot"
            android:label="@string/title_activity_voot"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".facebook"
            android:label="@string/title_activity_facebook"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".tutorialspoint"
            android:label="@string/title_activity_tutorialspoint"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".Instagram"
            android:label="@string/title_activity_instagram"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".hotstar"
            android:label="@string/title_activity_hotstar"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".feedback"
            android:label="@string/title_activity_feedback"
            android:theme="@style/FullScreen" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".mapactivty"
            android:label="@string/title_activity_mapactivty" />
        <activity android:name=".error" />
        <activity android:name=".home" />
        <activity
            android:name=".schitayuhome"
            android:label="@string/title_activity_schitayuhome"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>